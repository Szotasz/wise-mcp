{
  "type": "object",
  "required": ["WISE_API_TOKEN"],
  "properties": {
    "WISE_API_TOKEN": {
      "type": "string",
      "title": "Wise API Token",
      "description": "Personal API token from wise.com/settings/api-tokens"
    },
    "WISE_ENV": {
      "type": "string",
      "title": "Environment",
      "description": "Wise environment (live or sandbox)",
      "enum": ["live", "sandbox"],
      "default": "live"
    }
  }
}
